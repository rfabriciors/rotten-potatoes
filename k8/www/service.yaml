apiVersion: v1
kind: Service
metadata:
  name: movies
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: movies
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30000
  type: LoadBalancer
